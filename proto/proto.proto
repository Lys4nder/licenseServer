syntax = "proto3";

package ImageData;

message Image {
    bytes image = 1;
}

message ImagesResponse {
    repeated Image images = 1;
}

service ImageService {
    rpc UploadImageRequest(stream Image) returns (stream ImagesResponse);
}